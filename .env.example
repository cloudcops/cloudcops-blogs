# Outrank webhook shared secret (set the same value in Outrank dashboard)
OUTRANK_ACCESS_TOKEN=

# GitHub fine-grained personal access token (scoped to cloudcops-blogs repo)
# Required permissions: Contents (read+write), Pull Requests (read+write)
GITHUB_TOKEN=
